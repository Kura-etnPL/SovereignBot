# V4.2 P6.2 Release / Update — Local RC Acceptance

Date: 2026-09-02
Workspace: `E:\Eternal\Auto_Empire\worktrees\sovereign-p1-handoff-protocol`
Network: disabled by task boundary; no fetch, upload, GitHub Release, push, or PR mutation.

## Implemented boundary

- Core/Desktop/runtime/vendor version is frozen at `4.0.0`; `desktop/package-lock.json` root metadata is checked by `npm run release-version`.
- Electron Forge + Squirrel remains the only Windows packaging path. Fuses are flipped and verified before Squirrel output. Stable packaging requires environment-injected certificate file/password and fails closed without them. Local RC uses `SOVEREIGNBOT_RELEASE_MODE=rc` and is explicitly unsigned when no signer is injected.
- `out/release-manifest.json`, `out/SHA256SUMS.txt`, and `out/release-publish-command.txt` are generated from the local build. The manifest binds source provenance, versions, Electron distribution pin, internal Node, vendored Core, fuses, channel, signature state, and artifact hashes. Publication remains an explicit maintainer action.
- Settings defaults to Stable and exposes Preview/Off plus explicit Check, Stage, and Apply-on-restart actions. Main-process update validation rejects schema mismatch, channel mismatch, downgrade, minimum-version violation, missing backup requirement, unsigned stable metadata, traversal/symlink artifacts, and bad size/SHA-256. Applying requires the P6 pre-update backup and records Attention on failure.
- Installer E2E is prepared to use a worktree-local temporary root and covers silent fresh install, installed packaged smoke, V3 fixture migration with pre-migration backup, restart idempotency, same-package upgrade, and injected migration failure with V3 preservation plus Attention. Because Squirrel `Setup.exe` resolved the host profile despite an injected `LOCALAPPDATA` in this workstation, the script now refuses to run unless a disposable Windows profile explicitly proves isolation.

## Offline verification record

- `npm run release-version`: PASS.
- Desktop targeted release/update/version tests: PASS (60 tests in the filtered run; remaining files intentionally skipped by name pattern).
- Desktop syntax check: PASS (152 files).
- Root release/version tests: PASS in the filtered run.
- Bundled internal Node, Forge, and Electron cache: AVAILABLE.
- `signtool.exe`: unavailable in the local PATH; Authenticode verification: SKIP. No certificate/password was supplied or committed. Stable gate remains fail-closed; unsigned RC is the only local release mode accepted in this environment.
- `npm run make`: PASS with cached tools. `npm run smoke:packaged`: SKIP/FAIL in this headless local session after the packaged child produced no JSON for 180s, including with fake-provider fixtures. Installer E2E: SKIP by safety guard after Squirrel was observed resolving the host profile; no further install was attempted. No network fallback was used.

The recorded local RC is a dirty-source diagnostic artifact, not an uploadable formal release: `sourceHeadSha=2910e15e5ef2da4b0d2e539aaa8e432e1f30d7a3`, `sourceTreeState=dirty`, and `publishEligible=false`. Its historical artifact hashes below are retained for diagnosis only; they must not be described as bytes produced by the final amended commit.

## Local RC artifact record

Generated under the ignored dedicated-worktree path `desktop/out` with `SOVEREIGNBOT_RELEASE_MODE=rc` and `SOVEREIGNBOT_RELEASE_CHANNEL=preview`:

| Artifact | Relative path | Size | SHA-256 |
|---|---|---:|---|
| Setup.exe | `out/make/squirrel.windows/x64/SovereignBot-4.0.0 Setup.exe` | 177930240 | `6f77c49ec00dd56a713122e44a910a947f9ef1697183f81b3a8597bc5f9a80af` |
| Full package | `out/make/squirrel.windows/x64/sovereignbot-4.0.0-full.nupkg` | 177397315 | `8c82af5fb05e05e4529e85fb657a6bd303f8c1eb82f67dea5ffefc21fd44114b` |
| RELEASES | `out/make/squirrel.windows/x64/RELEASES` | 83 | `de3fd2bfccd327692683c44ab5e11893f67fe27867b266d9cfd61cb9a84ced4c` |
| Packaged executable | `out/SovereignBot-win32-x64/SovereignBot.exe` | 235534336 | `9beb4e52242e38e0ceeae03a8056088ec1e301682b7287efebda931ad0b60a6c` |
