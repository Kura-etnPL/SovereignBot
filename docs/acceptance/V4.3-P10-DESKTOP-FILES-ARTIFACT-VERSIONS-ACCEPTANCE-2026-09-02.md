# V4.3 P10 — Files & Artifacts Version Lifecycle Acceptance

Date: 2026-09-02
Branch: `codex/v4-review-directed-handoff-protocol`
Source commit: `61017421fc3ccc02fc31ffe91571ad432d9c726a`

## Delivered slice

The local Files & Artifacts path now supports:

`Files & Artifacts → inspect / preview / open / reveal → History → Restore as new version`

- Existing ArtifactStore records are backward-compatible: old entries receive family `artifactFamilyId` and `version: 1` on load.
- Restore copies bytes from the main-process managed artifact into a new immutable record in the same `artifacts.json` and managed storage, with `artifactFamilyId`, `version`, and `parentArtifactId`.
- The Hub shows the latest public version per family while preserving the existing Conversation, workspace/Project association, Team, Channel, and Coworker projections.
- History and restore requests are enumerated, bounded, identifier-only IPC channels; renderer code never receives a filesystem path.
- Restored content is public only because the source artifact was already public; no upload, sharing, cloud sync, retention, deletion, scheduler, provider, OAuth, or capability path was added.
- Restores invalidate the existing Search index through the established mutation channel set.

## Acceptance evidence

Command:

```text
node scripts/verify-v47-artifact-lineage.mjs
```

Result: PASS — hidden real Electron gate `14/14`.

The gate verified that:

- the Electron window remained hidden and used an isolated temporary profile;
- the real Files & Artifacts page rendered Preview, History, Restore-as-new-version, and Conversation navigation actions;
- the History action read the initial v1 timeline and Restore created visible v2 without removing v1;
- v2 retained the artifact family, parent, Conversation, Coworker, and workspace lineage;
- source managed bytes remained unchanged;
- public list, history, Hub, and restart projections contained no raw paths, URLs, provider/account/session/credential data, coordinates, driver details, or authority fields;
- rebuilding the local stores and rebinding the same hidden renderer recovered both versions and selected v2 as the Hub projection.

Additional local checks:

| Check | Result |
| --- | --- |
| Desktop syntax | PASS — 164 files |
| Desktop full test suite | PASS — 251 passed, 0 failed |
| Artifact/IPC/Product focused tests | PASS — 10 passed, 0 failed |
| Secret scan | PASS — clean |
| `git diff --check` | PASS |
| Network/install/provider activity | Not used by P10 |
| Local RC signing | Unsigned; `publishEligible=false` |

Raw gate output and the JSON manifest are retained in [`_evidence_v47_2026-09-02`](../../_evidence_v47_2026-09-02/). No installer, release artifact, stable publication, push, PR, or merge is claimed.
