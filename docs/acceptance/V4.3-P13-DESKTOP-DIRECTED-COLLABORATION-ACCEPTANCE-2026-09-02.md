# V4.3 P13 — Directed Team Collaboration Acceptance

Date: 2026-09-02

Branch: `codex/v4-review-directed-handoff-protocol`

Source commit: `a7d28efe0469ccf844cf135e42db654044c132a5`

## Correction notice

The prior evidence in `_evidence_v51_2026-09-02` was insufficient for this acceptance: it proved only the first renderer-to-IPC handoff and restart, while review approval, handback, and the requested invalid/forged IPC cases were not exercised by the hidden real-Electron gate. The source implementation was not broadened; this correction adds the missing real-boundary verifier coverage and records fresh evidence below.

## Delivered slice

Team Channel details now provide a bounded collaboration control for the current owner:

- send a bounded task to any active teammate in the same Team;
- request a review from any active teammate, including a non-hardcoded Specialist;
- return ownership through the same explicit handoff action after a review result.

The main process derives the current owner from durable Team flow state and validates the managed Channel, target membership, active availability, runtime preflight, single-owner invariant, and duplicate/runaway guards. Renderer input is limited to `conversationId`, `targetCoworkerId`, `handoffType`, `reason`, and `boundedTask`; forged owner, provider, capability, token, path, or session authority is rejected by the existing validated IPC boundary.

Successful requests reuse the existing TeamService protocol, collaboration ledger, Conversation store, dispatcher wake path, and public activity projection. The owner changes once to the selected target, the bot-to-bot message mentions only that target, and no new executor, workflow engine, App/Skill grant, Governor authority, or provider run is introduced. Safe bounded task/reason fields persist in the active protocol and survive restart.

## Corrected acceptance evidence

Command:

```text
node scripts/verify-p13-team-collaboration.mjs
```

Result: PASS — hidden real Electron gate `36/36`.

The gate verified:

- hidden Electron window, sandboxed preload, validated IPC, and real Team Channel renderer controls;
- available teammate selection by public names only, with no runtime internals in the control surface;
- inactive, self, and non-roster targets rejected through the same renderer/preload/IPC action, with owner, ledger, and delivery fingerprints unchanged;
- forged `ownerId`, `providerAccountId`, `capability`, `token`, `workspacePath`, and `sessionId` rejected by the validated IPC boundary before the collaboration handler, with no owner or ledger overreach;
- duplicate active request rejection without a second durable request or delivery;
- a non-hardcoded active Specialist becoming the sole current owner for the initial handoff;
- the existing trusted protocol completing/submitting that bounded handoff;
- a review request through the same renderer/preload/IPC session to another active Specialist, with only that target receiving delivery;
- trusted review acceptance, submission, and approval leaving exactly one reviewer owner;
- a handback through the same renderer/preload/IPC session to the original implementation Specialist, with only that target receiving delivery;
- durable handoff activity and authority-free public Team/Channel projections;
- final owner, bounded task, protocol, and target delivery surviving renderer/service restart.

Additional local checks:

| Check | Result |
| --- | --- |
| Desktop syntax | PASS — 173 files |
| Desktop full test suite | PASS — 256 passed, 0 failed |
| Collaboration ledger tests | PASS — 15 passed, 0 failed |
| Secret scan | PASS — clean |
| `git diff --check` | PASS |
| Network/install/provider activity | Not used by P13 |
| Local RC signing | Unsigned; `publishEligible=false` |

Raw corrected gate JSON and log plus the corrected evidence manifest are retained in [`_evidence_v52_2026-09-02`](../../_evidence_v52_2026-09-02/). The prior `_evidence_v51_2026-09-02` record is retained as historical evidence and is superseded for P13 acceptance by this correction. No installer, release artifact, stable publication, push, PR, or merge is claimed.
