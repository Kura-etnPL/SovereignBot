# V4.3 P18 — Desktop Channel-Unread Notifications Acceptance

Date: 2026-09-03
Branch: `codex/v4-review-directed-handoff-protocol`
Baseline commit: `5326d4f45ff94613343a0e7b24c922b84f54620a`
P18 Source commit: `19d5421`
P18 Test & Dispatcher Verification commit: `b5295b2`

## Delivered Slice & Architectural Closure of P17 Limitation

SovereignBot Desktop P18 delivers a trusted production channel-unread notification producer that closes the explicit P17 limitation recorded in `handoff/HANDOFF.md`.

The implementation directly integrates the existing `ConversationStore`, `TeamService`, and `NotificationService` without introducing a second event system, database, scheduler, or generic renderer-create IPC.

### Architecture and Reviewer-Directed Hardening:

1. **Fail-Closed Intent Architecture (Zero Text/Regex Heuristics)**:
   - Eliminated text-matching and regex-based heuristics (`isInternalOrHarnessNoise` was rejected and removed).
   - Default fail-closed: coworker messages produce a channel-unread notification strictly when explicit call-site metadata sets `notifyChannelUnread === true`. If `undefined` or `false`, no notification is produced.
   - Audited production callsites across `coworker-dispatcher.js` and `team-service.js`: fanout child/review/join stages, collaboration requests, intermediate playbook steps, and handoff turns are explicitly marked with `internal: true, notifyChannelUnread: false`.
   - Only published, terminal, user-facing results (both standard unmanaged teams and managed playbook sequences via `previewHandoffDecision`) opt in to `notifyChannelUnread: true`.

2. **Real Production-Path Multi-Stage Dispatcher Verification**:
   - Sol independent review gap resolved: verified that the actual coworker dispatcher marks every intermediate stage `notifyChannelUnread: false` and the final user-relevant stage `notifyChannelUnread: true`.
   - In `collaboration-ledger.test.mjs` and `p18-channel-unread.test.mjs`, exercised a realistic 4-stage chain (Chief handoff -> Researcher handoff -> Coder review/handoff -> Chief final synthesis) with real `createNotificationService` and `createChannelUnreadProducer`. Proved that all intermediate handoff/review stages produce 0 channel-unread notifications, while the final synthesis produces exactly 1 notification (`title: "Project Channel · Chief"`, `body: "Chief synthesized the result."`).
   - In `collaboration-ledger.test.mjs`, exercised controlled fanout (Child 1, Child 2, Review, Join request) and proved that all intermediate child/review turns produce 0 notifications, while final fanout join produces exactly 1 coalesced notification (`body: "Chief joined the approved specialist results."`).
   - Proved in-place coalescing: subsequent user-facing completions in the same channel update the card body without duplicate entries.

3. **Managed Review Flow Legitimacy Inspection**:
   - Inspected repository behavior across `team-service.js` (`handoffOrder`, `indexForFlow`, `stageForIndex`, `resolveHandoff`) to determine whether any managed review flow can legitimately terminate at the reviewer without later synthesis.
   - Finding: review stages are strictly intermediate (`order.length - 2`, `stage === "reviewer"`). Review approval hands off to the owner for final synthesis (`order.length - 1`, `stage === "synthesis"`). No review flow legitimately terminates at the reviewer without synthesis.
   - Conclusion: `isReview` intermediate suppression (`isIntermediateStage: true, notifyChannelUnread: false`) is architecturally sound and preserved without speculative modification.

4. **Read-Only `conversation:get` and Explicit Acknowledgment Path**:
   - `conversation:get` is strictly read-only and never resolves unread state.
   - Added bounded `conversation:acknowledge` IPC handler calling `notifications.resolveChannelUnread(conversationId)`.
   - Schema validation in `v3-ipc-schema.js` enforces exact payload: `{ conversationId }` matching valid ID pattern, rejecting arbitrary properties or authority.

5. **Promise-Chained UI Acknowledgment & Badge Synchronization**:
   - In `desktop/ui/app.js`, `markConversationRead` saves local read markers synchronously to prevent UI latency.
   - Invokes `window.sovereignbot.conversations.acknowledge`, promise-chains the IPC call, catches async errors safely without crashing the UI, and dispatches `sovereignbot:refresh-notifications-badge` strictly after backend persistence settles.
   - All channel-opening flows (sidebar channel click, channels view click, Notification Center card navigation button, and direct navigation events) converge on this path.

6. **Security, Privacy & Redaction Preserved**:
   - Internal keys are never exposed to renderer projections or IPC arguments.
   - Stable dedupe keys are hashed to sha256 digests (`k_[a-f0-9]{32}`) on disk.
   - Secret tokens (`sk-`, `ghp_`, Bearer, Basic, Cookie) and Windows/Unix absolute paths are redacted at the service boundary before persistence, public projection, or popup delivery.

## Acceptance Evidence

Evidence directory: [`_evidence_v57_2026-09-03`](../../_evidence_v57_2026-09-03/)

- [`verify-p18-channel-unread.json`](../../_evidence_v57_2026-09-03/verify-p18-channel-unread.json) — machine-readable test results (10/10 PASS).
- [`verify-p18-channel-unread.log`](../../_evidence_v57_2026-09-03/verify-p18-channel-unread.log) — execution log from real hidden Electron gate.

### Verification Suite Results:

- `npm run verify:p18-channel-unread` (desktop) → **10/10 PASS** (Electron exit code 0)
- `npm run verify:p17-notification-center` (desktop) → **20/20 PASS** (Electron exit code 0)
- `node --test desktop/test/p18-channel-unread.test.mjs` → **10/10 PASS** (0 fail)
- `node --test desktop/test/collaboration-ledger.test.mjs` → **15/15 PASS** (0 fail)
- `npm test` (desktop) → **277/277 PASS** (0 fail)
- `npm test` (root) → **234/234 PASS** (232 pass, 2 skipped, 0 fail)
- `npm run check` (desktop) → **syntax ok: 192 files**
- `npm run secret-scan` (desktop) → **clean**
- `git diff --check` → **clean**

The local evidence is unsigned and not publishable: `publishEligible=false`. The evidence commit is the final commit containing this document, the updated handoff, and `_evidence_v57_2026-09-03`.

## Honest Operational Scope

- Channel unread notifications are produced for active coworkers in managed channels when terminal results complete.
- No remote network requests, external notification servers, cloud services, deployment, or account changes were made.
- Work was conducted strictly within `E:\Eternal\Auto_Empire\projects\SovereignBot-luna-attention` on branch `codex/v4-review-directed-handoff-protocol`.
