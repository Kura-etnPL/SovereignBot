# V4.3 P31 — Team Pack Gallery Consistency Acceptance

Date: 2026-09-03  
Branch: `codex/v4-review-directed-handoff-protocol`  
Source commits: `a0630f2370bbae8c20428c30ca852f5800808bc3` (`fix(desktop): unify Team Pack gallery editing`), `e88abdc2c0d5c6ee854921c511274bab8ce82d59` (verifier-only click-source preflight), `8ab0c0cbd3139c9be0ac128b837e797af3b3d57b` (invoke-based expression correction), `71c5dc38025bd2929f1354605e0f980e2cb070e4` (bounded native-input verifier path), `71e76be0a5ee789c9dd465ea97c8e54b7bb3036f` (scroll-then-native-input correction), `2000ed8afb0c06f3f77630dcd3dfca473e45befa` (renderer diagnostics and bounded state probe), `4303ea707c511bc3498a04559b1ef0b308fa5d2b` (probe invoke wrapping correction), `1ed28ffbfbe15c5b53805f19557f044a78716380` (all-call invoke preflight)

## Scope

The P28 inventory identified two ordinary Team Pack Gallery entries: the embedded Product hubs gallery (`nav-product-hubs` → `renderPacks`) and the dedicated Team Packs page (`nav-team-packs` → `packs`). The embedded path was reachable and still used a custom-name `window.prompt` edit plus clipboard export. It now exposes the same structured `Edit recipe / 编辑配方` and native `Export / 导出` behavior as the dedicated page through a small event bridge.

Both entries reuse the existing structured editor, `team:editPack` typed preload/IPC path, canonical product-surface service, and native `team:exportPackViaDialog` JSON file flow. No store, service, editor, authority, clipboard, or path API was added. First-party recipes remain read-only and retain Duplicate guidance; custom/imported recipes remain editable; the existing in-dialog validation/error and Cancel-without-write behavior remains in the shared editor.

## Acceptance matrix

| Requirement | Evidence |
|---|---|
| Older Product hubs gallery is an ordinary reachable entry | Source call chain: `nav-product-hubs` → first controller `refresh()` → `renderPacks()`; unsandboxed hidden Electron gate reached the real entry and enumerated 7 cards |
| Dedicated Team Pack page remains an ordinary reachable entry | Existing `nav-team-packs` → `nav("team-packs")` → `packs()` path; real gate reached the page and completed its later edit/export/import/restart checks |
| Old custom Edit no longer uses name prompt | `desktop/test/p31-team-pack-gallery.test.mjs` P31 static invariant; legacy renderer delegates `sovereignbot:open-team-pack-editor` |
| Old Team Pack Export no longer uses clipboard | P31 static invariant; legacy renderer delegates `sovereignbot:export-team-pack` to `exportPackToFile` |
| Both entries reuse one structured editor and one typed service/IPC path | P31 static invariant plus shared event handlers calling `openPackEditor`; save remains `api.teams.editPack` |
| Both entries use native bounded JSON export/import | Existing P20/P21 native file path plus shared `exportPackToFile`; real gate passed old-entry native export and authority-bearing import rejection |
| First-party read-only; Duplicate guides custom editing | P31 static invariant and existing P21 gate contract |
| Custom/imported editable; Cancel no write; product error state | Real gate passed old-entry structured edit and Cancel no-write; dedicated-page consistency assertion failed because the observed title remained `Product Discovery Team` before its later successful edit |
| Authority-bearing import remains rejected | Real gate passed product-UI rejection without exposing the fixture path |
| Restart persistence uses the existing store | Real gate passed persisted dedicated-page edit and restarted older-gallery title checks |

## Verification

- `node --test desktop/test/p31-team-pack-gallery.test.mjs` — **4/4 PASS**.
- `npm test` (desktop) — **300 pass, 1 skipped, 0 fail**.
- `npm run check` — syntax ok, **228 files**.
- `npm run secret-scan` (desktop) — clean.
- `git diff --check` — clean.
- Earlier sandboxed `npm run verify:p31-team-pack-gallery` runs were blocked before UI assertions by GPU subprocess exit `-1073741515` and `ERR_FAILED (-2) loading 'sovereignbot://app/'`; the existing P21 Team Pack gate reproduced that environment failure. The single follow-up unsandboxed run loaded the app protocol and produced **12/13 PASS**: hidden-window, older-gallery reachability/read-only, Duplicate, old-entry structured edit, Cancel no-write, shared persisted edit, native export, authority-bearing import rejection, and restart persistence all passed. The only failed assertion was dedicated-page consistency: the custom card exposed Edit but still displayed `Product Discovery Team` instead of the expected `P31 Old Gallery Edited` before its later successful dedicated-page edit. Renderer console recorded only CSP warnings; no `render-process-gone` event occurred.
- Evidence: `_evidence_p31_2026-09-03/verify-p31-team-pack-gallery.json` and `.log`; the record contains the final 12/13 checks, Duplicate id, failed consistency observation, native-input marker, renderer console, and renderer-health result. No screenshot was produced because the verifier recorded a failed acceptance assertion.

No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed. The source changes are committed; P31 remains Pending solely because the dedicated-page consistency assertion failed, despite the later edit/export/import/restart checks passing.
