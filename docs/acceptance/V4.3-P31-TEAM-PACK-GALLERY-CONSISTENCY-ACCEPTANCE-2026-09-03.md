# V4.3 P31 — Team Pack Gallery Consistency Acceptance

Date: 2026-09-03  
Branch: `codex/v4-review-directed-handoff-protocol`  
Source commits: `a0630f2370bbae8c20428c30ca852f5800808bc3` (`fix(desktop): unify Team Pack gallery editing`), `e88abdc2c0d5c6ee854921c511274bab8ce82d59` (verifier-only click-source preflight), `8ab0c0c` (invoke-based expression correction)

## Scope

The P28 inventory identified two ordinary Team Pack Gallery entries: the embedded Product hubs gallery (`nav-product-hubs` → `renderPacks`) and the dedicated Team Packs page (`nav-team-packs` → `packs`). The embedded path was reachable and still used a custom-name `window.prompt` edit plus clipboard export. It now exposes the same structured `Edit recipe / 编辑配方` and native `Export / 导出` behavior as the dedicated page through a small event bridge.

Both entries reuse the existing structured editor, `team:editPack` typed preload/IPC path, canonical product-surface service, and native `team:exportPackViaDialog` JSON file flow. No store, service, editor, authority, clipboard, or path API was added. First-party recipes remain read-only and retain Duplicate guidance; custom/imported recipes remain editable; the existing in-dialog validation/error and Cancel-without-write behavior remains in the shared editor.

## Acceptance matrix

| Requirement | Evidence |
|---|---|
| Older Product hubs gallery is an ordinary reachable entry | Source call chain: `nav-product-hubs` → first controller `refresh()` → `renderPacks()`; unsandboxed hidden Electron gate reached the real entry and enumerated 7 cards |
| Dedicated Team Pack page remains an ordinary reachable entry | Existing `nav-team-packs` → `nav("team-packs")` → `packs()` path; source/static coverage retained, but the real gate stopped before this later path |
| Old custom Edit no longer uses name prompt | `desktop/test/p31-team-pack-gallery.test.mjs` P31 static invariant; legacy renderer delegates `sovereignbot:open-team-pack-editor` |
| Old Team Pack Export no longer uses clipboard | P31 static invariant; legacy renderer delegates `sovereignbot:export-team-pack` to `exportPackToFile` |
| Both entries reuse one structured editor and one typed service/IPC path | P31 static invariant plus shared event handlers calling `openPackEditor`; save remains `api.teams.editPack` |
| Both entries use native bounded JSON export/import | Existing P20/P21 native file path plus shared `exportPackToFile`; P31 static coverage retained, but the real gate stopped before native I/O |
| First-party read-only; Duplicate guides custom editing | P31 static invariant and existing P21 gate contract |
| Custom/imported editable; Cancel no write; product error state | Shared P21 editor implementation and existing P21 gate contract; P31 real gate stopped at Duplicate injection before this later coverage |
| Authority-bearing import remains rejected | Existing P20/P21 authority-boundary implementation and P31 gate check; P31 real gate stopped at Duplicate injection before this later coverage |
| Restart persistence uses the existing store | Existing product-surface service/store; P31 real gate stopped at Duplicate injection before this later coverage |

## Verification

- `node --test desktop/test/p31-team-pack-gallery.test.mjs` — **4/4 PASS**.
- `npm test` (desktop) — **300 pass, 1 skipped, 0 fail**.
- `npm run check` — syntax ok, **228 files**.
- `npm run secret-scan` (desktop) — clean.
- `git diff --check` — clean.
- Earlier sandboxed `npm run verify:p31-team-pack-gallery` runs were blocked before UI assertions by GPU subprocess exit `-1073741515` and `ERR_FAILED (-2) loading 'sovereignbot://app/'`; the existing P21 Team Pack gate reproduced that environment failure. The final unsandboxed run loaded the app protocol, reached the older Product hubs gallery, and passed the hidden-window, gallery-reachability, and first-party read-only/Duplicate checks (3/4 checks). The verifier's Node preflight for the exact `async()=>{...button.click()...}` expression passed, but the existing `invoke()` execution returned generic renderer `Script failed to execute` at the Duplicate step. This is a verifier/runtime injection failure, not a completed product click-path acceptance.
- Evidence: `_evidence_p31_2026-09-03/verify-p31-team-pack-gallery.json` and `.log`; the record contains the latest unsandboxed checks, exact click expression, Node preflight result, and fatal error. No screenshot was produced because the verifier stopped at Duplicate injection.

No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed. The source changes are committed; real Electron Duplicate/edit/export/import/restart click-path acceptance remains pending a working verifier/runtime injection path.
