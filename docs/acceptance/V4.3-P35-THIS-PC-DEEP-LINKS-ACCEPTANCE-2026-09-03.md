# V4.3 P35 — This PC product-loop acceptance

Date: 2026-09-03
Source commit: `f4cb455` (`fix(desktop): close This PC product loop`)

## Audit decision

The existing ordinary This PC entry, safe Coworker projection, current app/site fields, shared/private context labels, live-frame/page-detail calls, Take Over/Hand Back service path, artifact list, and Computer History projection were present. Existing `thisPc.frame` remains the safe projection over the governed lifecycle; no new capture stack, OS permission, external application, browser automation, lease, or target model was added.

The highest-value local gap was the final product loop: the strict app-protocol allowlist omitted `this-pc-ui.js` and `this-pc.css`, and the CSP did not allow the custom local app scheme, so the ordinary Electron window could expose the entry markup/API while silently losing the loaded product behavior. After loading was restored, This PC artifact/activity actions also discarded their Coworker deep-link context.

## Change

- Allowlist the existing This PC JavaScript and stylesheet and permit only the existing `sovereignbot:` local scheme in the renderer CSP.
- Preserve opaque `coworkerId` on the This PC artifact event.
- Carry artifact/history deep-link scope into the existing Product Hubs filters, show an in-product scope notice, and guard against stale refreshes overwriting the selected scope.
- Add the existing Computer History projection’s bounded `coworkerId` filter to typed IPC and the main-process service.
- Keep all renderer data limited to public labels and opaque identifiers; no raw paths, coordinates, handles, profiles, credentials, sessions, or generic IPC were introduced.

## Evidence

`_evidence_p35_2026-09-03/verify-p35-this-pc-deep-links.json` and `.log` record the real hidden Electron gate: **11/11 PASS**.

The gate covered hidden ordinary entry, safe status/health/context/app/site rendering, safe projection leakage checks, Coworker-scoped activity and artifact deep links, opaque IPC arguments, empty state, failure state, and renderer/service restart entry persistence. The existing governed Take Over/Hand Back authority and restart proof remain covered by `desktop/test/this-pc-canary.test.mjs`.

Additional checks:

- `node --test desktop/test/app-assets.test.mjs desktop/test/product-surfaces.test.mjs desktop/test/v3-product-shell.test.mjs desktop/test/this-pc-canary.test.mjs` — 27/27 PASS
- `npm test` — 306 tests: 305 pass, 1 skipped, 0 fail
- `npm run check` — syntax ok, 242 files
- `npm run secret-scan` — clean
- `git diff --check` — clean

Evidence is local, unsigned, and `publishEligible=false`. No provider, network, OAuth, account, browser, user-data, packaging, deployment, publish, push, or main-merge action was performed.
