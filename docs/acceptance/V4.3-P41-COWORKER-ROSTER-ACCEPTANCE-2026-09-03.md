# V4.3 P41 — 50+ Coworker roster acceptance

Date: 2026-09-03
Scope: `desktop/ui/index.html`, `desktop/ui/app.js`, `desktop/ui/style.css`, and the P41 local acceptance harness only.
Boundary: `LOCAL_FIXTURE`; `publishEligible=false`; no provider, login, network, deployment, or publishing action.

## Delivered behavior

- The Coworkers sidebar renders a bounded initial window of 14 non-archived Coworkers.
- Search matches safe name/role text and can reach a Coworker outside the initial crop.
- Status filtering and bilingual counts cover active, working, available, attention, and paused states.
- “Show more” expands the complete local roster and collapse restores the bounded window.
- The selected direct Coworker is retained in the bounded view after selection.
- Provider roster data is consumed only through the safe `ready` binding projection; no agent, account, session, workspace, or provider internals are rendered.
- Refresh and a recreated CoworkerStore preserve the roster and remote-search target.

## Verification

- Focused canary: `node --test desktop/test/coworker-roster.test.mjs` — pass.
- Desktop unit suite: `npm test` — 312 pass, 1 skipped, 0 failed.
- Desktop source check: `npm run check` — `syntax ok: 262 files`.
- Desktop secret scan: `npm run secret-scan` — clean.
- `git diff --check` — pass.
- Hidden Electron gate: Sol independently ran `npm run verify:p41-coworker-roster` in the local no-sandbox environment; all 9 checks passed and Electron exited with code 0. The Luna-host Chromium attempt was blocked by `ERR_FAILED (-2)` and was not retried; it does not change the successful independent runtime acceptance.

## Evidence

The machine-readable output is `docs/acceptance/verify-p41-coworker-roster.json` with schema `sovereignbot.desktop.p41-coworker-roster-canary.v1`. It records `fixtureBoundary: LOCAL_FIXTURE`, `publishEligible: false`, `ok: true`, all 9 checks passing, and no external actions.
