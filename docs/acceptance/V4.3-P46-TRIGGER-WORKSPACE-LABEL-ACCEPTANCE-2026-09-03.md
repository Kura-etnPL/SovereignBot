# V4.3 P46 Trigger Workspace Label Acceptance

Date: 2026-09-03
Workspace: `E:\Eternal\Auto_Empire\projects\SovereignBot-luna-attention`
Source commit: `1ddec3d` (`feat(desktop): label trigger workspace selector safely`)

Network and external actions were disabled by task boundary. No GitHub, Cloudflare,
deployment, publish, installer, account, browser-login, or user SovereignBot data
action was performed.

## Delivered behavior

- Trigger workspace options use the existing safe `services.listWorkspaces()` public
  projection: recognizable `label`, with the existing safe `kind` mapping where
  applicable.
- The UI no longer reads or displays `workspace.path` or any raw absolute workspace
  path.
- Trigger creation still submits only the opaque `workspaceId` plus the governed
  `name`, `routineId`, and relative `pathPrefix` fields. No authority, provider,
  credential, or path field is accepted from the selector.
- The targeted regression covers the production Trigger UI source contract. The
  existing V44 hidden Electron gate was extended in place; no workspace subsystem or
  test-only architecture was added.

## Acceptance boundary

- Fixture boundary: `LOCAL_FIXTURE`.
- `publishEligible`: `false`.
- `externalActions`: `[]`.
- The gate used temporary local workspaces only: `trusted-workspace` and
  `reference-workspace`. It did not open or inspect user workspace data.

## Sol independent real Electron acceptance

Sol ran the exact local gate:

```powershell
cd E:\Eternal\Auto_Empire\projects\SovereignBot-luna-attention\desktop
npm run verify:p46-trigger-workspace-label
```

Result: Electron exit `0`, **41/41 checks PASS**.

The gate clicked the real Trigger form, observed both workspace labels, created the
file trigger, captured the renderer-to-main create payload, wrote a fixture file, and
verified the existing governed Routine → Job → Chief path. It also verified that the
payload contained only `name`, `routineId`, opaque `workspaceId`, and relative
`pathPrefix`; no raw absolute path or authority field was present. The existing
event-trigger safety checks additionally confirmed that file contents and absolute
workspace paths did not reach provider-facing instructions or trigger history.

Raw local gate evidence is retained at:

- [`verify-v44-event-triggers.json`](../../_evidence_v44_2026-08-30/verify-v44-event-triggers.json)
  — machine-readable 41/41 PASS summary and captured selector/payload checks.
- [`verify-v44-event-triggers.log`](../../_evidence_v44_2026-08-30/verify-v44-event-triggers.log)
  — raw hidden-gate output.
- [`verify-v44-event-triggers.png`](../../_evidence_v44_2026-08-30/verify-v44-event-triggers.png)
  — local Triggers UI screenshot.

The JSON/log retain bounded internal diagnostics such as temporary fixture
`dataDir`, watcher roots, and trusted main-process execution context. Those paths are
fixture-only diagnostic evidence, not the public `workspace:list` projection and not
data from a user workspace. The renderer evidence exposes only opaque workspace IDs,
labels, and relative event paths. Evidence inspection found no file-body canary,
credential-like material, provider/account data, or user SovereignBot data.

## Verification record

| Check | Result |
| --- | ---: |
| Sol hidden Electron P46 gate | PASS; Electron exit 0; 41/41 checks PASS |
| Focused Event Trigger/workspace/P46 tests | PASS; 12/12 |
| Desktop syntax check | PASS; 276 files |
| Secret scan | PASS; clean |
| `git diff --check` | PASS |

The source implementation and focused regression are in `1ddec3d`. This acceptance
document and the checked local gate evidence are a separate docs/evidence-only
closeout commit. The closeout remains local and unsigned; it is not eligible for
publication.
