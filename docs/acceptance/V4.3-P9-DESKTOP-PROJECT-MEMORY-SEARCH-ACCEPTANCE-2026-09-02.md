# V4.3 P9 — Project / Memory / Search Acceptance

Date: 2026-09-02
Branch: `codex/v4-review-directed-handoff-protocol`
Source commit: `519ca8fb53a9230c50dff4d4d05c11ff7b9ac0d1`

## Delivered slice

The local product path is now complete for:

`Project card → Add approved Project fact → durable Project Memory → Project-scoped Search → Memory deep link`

- Added a renderer-validated `memory:putFact` IPC path restricted to canonical Project ids and bounded title/content/tags.
- Added the Project card “Add fact” action and a real Memory form. Archived Projects remain read-only for fact writes.
- Added durable restart coverage and Search result `total`/`hasMore` metadata.
- Added stale-request protection in the Memory and Search renderers.
- Added the missing `memory-ui.js` app-protocol allowlist entry; the hidden Electron gate catches this real asset-wiring boundary.
- No provider, OAuth, session, workspace-path, credential, scheduler, authority, or metered capability was added.

## Acceptance evidence

Command:

```text
node scripts/verify-v46-project-memory-search.mjs
```

Result: PASS — hidden real Electron gate `15/15`.

The gate verified that:

- the BrowserWindow remains hidden;
- renderer preload creates a canonical `project_<16 hex>` Project;
- the Project page exposes Add fact and opens a Project-scoped Memory form;
- the approved fact appears in the real Memory UI and `memory:list` projection;
- Project-scoped Search finds the fact with `total: 1`, `hasMore: false`;
- Search deep-links back to and selects the Memory row;
- Project, Memory, and Search projections contain no raw path, Team id, provider, account, session, credential, token, cwd, or authority data;
- rebuilding the local services and reloading the same hidden renderer preserves the Project, fact, and rebuilt Search index.

Additional local checks:

| Check | Result |
| --- | --- |
| Desktop syntax | PASS — 161 files |
| Desktop full test suite | PASS — 250 passed, 0 failed |
| Focused Project/Memory/Search/Product tests | PASS — 24 passed, 0 failed |
| `git diff --check` | PASS |
| Network/install/provider activity | Not used by P9 |
| Local RC signing | Unsigned; `publishEligible=false` |

Raw hidden-gate output is retained in `_evidence_v46_2026-09-02/`. No installer, release artifact, stable publication, push, PR, or merge is claimed.
