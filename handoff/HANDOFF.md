# Auto_Empire Desktop Handoff

Updated: 2026-09-03

- Branch: `codex/v4-review-directed-handoff-protocol`
- P17 source commits: `bbfab400d742c562aee2369b981c01c7ece1d2ba`, `6e309d3`, `f49f7d3`
- P18 source commit: `19d5421`
- P18 regression test commit: `b5295b2`
- Scope completed: P18 Trusted Production Channel-Unread Notification Producer, closing the explicit P17 limitation. Integrates ConversationStore message events via observer API in the main process. Implements fail-closed intent architecture where coworker messages notify only when explicit production call-site metadata sets `notifyChannelUnread === true`. Suppresses user self-messages, inactive coworkers, archived channels, and intermediate handoff/review/fanout protocol stages. Tested via the real coworker dispatcher multi-stage chain (Chief -> Researcher -> Coder -> Chief) and a controlled local-runtime fanout join flow. The multi-stage test retains per-flush 0/0/0/1 notification assertions; the fanout test records every coworker message's production options and proves six child/review/protocol messages use `notifyChannelUnread: false`, with only the final join using `true`, while the real producer/service yields exactly 1 coalesced notification. This fanout fixture is not a live-provider or packaged-Electron proof. Keeps `conversation:get` strictly read-only and resolves unread state solely via explicit bounded `conversation:acknowledge` IPC. Promise-chains acknowledgement in UI and synchronizes badge refresh with backend resolution. Preserves digest dedupe key storage (`k_[a-f0-9]{32}`), collision-resistant opaque notification IDs (`notif_[a-f0-9]{16}`), and trusted-boundary redaction of secrets and absolute paths.
- Acceptance: `npm run verify:p18-channel-unread` → 10/10 PASS; `npm run verify:p17-notification-center` → 20/20 PASS; `node --test desktop/test/p18-channel-unread.test.mjs` → 9/9 PASS; `node --test desktop/test/collaboration-ledger.test.mjs` → 15/15 PASS; `npm test` (desktop) → 276 tests: 275 pass, 1 skipped, 0 fail; `npm test` (root) → 234/234 PASS (232 pass, 2 skipped, 0 fail); `npm run check` → syntax ok, 192 files; `npm run secret-scan` → clean; `git diff --check` → clean.
- Evidence: `_evidence_v57_2026-09-03/`; hidden Electron gate remains 10/10 PASS, and `p18-local-verification.json` records the current 9/9 focused P18 suite, 15/15 collaboration ledger, full-suite counts, and observed fanout intent boundary. Evidence is local, unsigned, and `publishEligible=false`; the fanout summary is a controlled local-runtime proof, not a live-provider or packaged-Electron proof. The evidence commit is the final commit containing this handoff, the P18 acceptance document, and the evidence directory; its SHA is reported in the delivery summary.
- Channel-unread status: P17 limitation closed. Automated trusted main-process producer is active and verified across real multi-stage dispatcher chains and hidden Electron runtime.
- No provider, network, OAuth, cloud, remote, packaging, deployment, or account action was performed.
- P19 Product Burst audit and slice: audited Playbook Library, Files & Artifacts Hub, Computer Activity History, Skill Library, Team Pack Gallery, Channels, and External Control. Implemented the highest-priority complete local slice: independent Channels page New/Edit now uses the governed full channel dialog with Team context, kind, instructions, shared workspace, and Playbook, then refreshes the page after persistence. Removed the limited prompt fallback and added async error handling. Source commit: `8725ade` (`Complete product channel editor flow`).
- P19 acceptance: real Electron `npm run verify:software-team` passed, including `P0_CHANNEL_PRODUCT_EDITOR_ROUNDTRIP`; desktop `npm run check` passed (192 files); focused Product Shell test passed 13/13; desktop `npm test` passed 275 with 1 Docker-dependent skip and 0 failures. Evidence: `_evidence_v58_2026-09-03/channel-editor-local-verification.json`; docs/evidence commit is the final commit containing the audit document, evidence record, and this handoff.
- P19 remaining priority order: production secure paired/relay External Control lifecycle; Team Pack file-picker/download roundtrip; independent Files/Artifacts, Computer History, and Skills UI/capability-boundary gates; then Playbook/Channels archive/template click-path gates. No package/deploy/publish/main merge or user account/browser operation was performed.
- P20 Team Pack local file flow: native main-process open/save dialogs now replace independent Gallery clipboard/prompt JSON import/export. File content is bounded to 64 KiB, validated through the existing declarative `team:importPack` schema, rejects authority-bearing fields, sanitizes file I/O errors, and exposes only basenames to the renderer. Gallery refreshes after import and preserves existing Install, Duplicate, and custom Edit service paths. Source commit: `839a314` (`Complete Team Pack local file flow`).
- P20 acceptance: `npm run verify:p20-team-packs` → hidden Electron gate 23/23 PASS; `npm test` (desktop) → 278 pass, 1 skipped, 0 fail; `npm run check` → syntax ok, 194 files; `npm run secret-scan` → clean; `git diff --check` → clean. Evidence: `_evidence_v20_2026-09-03/`; local unsigned evidence with `publishEligible=false`. The existing custom Edit native prompt remains unchanged; the hidden gate proves Edit control reachability and the same preload IPC persistence contract separately because the hidden Electron runner cannot inject native prompt text.
- P21 Team Pack structured recipe editor: source commit 72897c2 (Implement structured Team Pack recipe editor). Independent Gallery custom/imported Edit now opens a fixed product-internal form for pack metadata, coworker names/roles/instructions and safe model profiles, channel metadata/instructions/playbook links, and playbook metadata/ordered steps/semantic fields; bounded add/remove actions are supported. First-party recipes remain read-only and are duplicated before editing. Save uses the existing team:editPack preload/IPC path, refreshes after success, surfaces errors in-dialog, and cancel performs no write. Hidden Electron real click-path gate passed 27/27 for both verify:p20-team-packs and verify:p21-team-packs; it verified full composition persistence, refresh, cancel no-write, authority-bearing import rejection, and isolated restart persistence. Desktop full suite: 278 pass, 1 skipped, 0 fail; focused Product Shell: 13/13 PASS; check: 194 files; secret scan and diff check clean. Evidence: _evidence_v21_2026-09-03/; local unsigned, publishEligible=false. No provider, network, account, browser, external-control, packaging, deployment, or publish action was performed.
- P22 Playbook Library structured editor and native file flow: source commit `a35e8d0` (`Implement Playbook Library structured editor and file flow`). Independent Playbooks page New/Edit now uses a fixed form for name, description, ordered steps, stages, review points, expected output, recommended coworker roles, and recommended Skills; bounded add/remove/reorder controls preserve the existing backend limits, Cancel performs no write, and errors remain in-dialog. Team/Channel assignment remains typed and archived Playbooks hide assignment controls. Duplicate, Archive, and Restore use the existing service path. Native main-process Electron dialog helpers replace renderer JSON prompt/clipboard import/export, cap files at 64 KiB, validate `sovereignbot.desktop.playbook.v1`, return only basenames/status, and reject authority/path/session/token material. Fixed reload validation preserves unassigned duplicate/import entries across restart. Hidden Electron gate `npm run verify:p22-playbooks` passed 11/11; focused suite passed 31/31; `npm run check` passed with 199 files; desktop full suite passed 282 with 1 skip; root suite passed 232 with 2 skips; secret scan clean. Evidence: `_evidence_v22_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P23 Skill Library structured editor and native file flow: source commit `0be3077fa131b9f377da48c0bb5b9094bdf9a335` (`feat(desktop): complete structured Skill Library path`). Independent Skills page New/Edit now uses a structured form for name, description, instructions, source/type, inputs, ordered steps, expected output, validators, and requested capability declarations; bounded add/remove/reorder controls preserve backend limits, Cancel performs no write, and errors remain in-dialog. Archived Skills are read-only until restored. Duplicate, Archive/Restore, Retest definition, Team/Coworker assign/unassign, and Create Routine remain wired to typed service/event paths. Native main-process JSON import/export is capped at 64 KiB, returns only basename/status data to the renderer, validates the declarative `sovereignbot.desktop.skill.v1` shape, and rejects authority/path/session/token/credential/URL material; requested capabilities never grant access. Fixed governed-retest persistence so `lastTestedAt` and `updatedAt` survive restart while lifecycle metadata stays separate from the portable definition. Hidden Electron gate `npm run verify:p23-skills` passed 14/14; focused suite passed 21/21; `npm run check` passed with 205 files; desktop full suite passed 288 with 1 skip; root suite passed 232 with 2 skips; secret scan and diff check clean. Evidence: `_evidence_v23_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P24 Files & Artifacts and Computer Activity History product path: source commit `13e158e59d593bb82b125b4070b99887347c691c` (`feat(desktop): close Artifacts and Computer History product path`). Independent Artifacts now uses an in-product bounded preview dialog with explicit readable, truncated, binary-unavailable, and sanitized read-failure states; Recent/team/channel/coworker/MIME filters stay bounded and selected filters survive refresh. Exact `sovereignbot:open-artifact` navigation focuses the requested card or reports a safe unavailable message. Existing typed History, Restore-as-new-version, native-dialog Revise, managed Open/Reveal, and source-conversation navigation paths are preserved. Computer History reuses the existing runtime audit and allowlisted `product-surface-service` projection without creating a second log store; secret/auth/session/continuity and non-allowlisted fields remain excluded. Hidden Electron gate `npm run verify:p24-artifacts` passed 15/15; focused suite passed 29/29; desktop full suite passed 291 with 0 skips; root suite passed 232 with 2 skips; secret scan and diff check clean. Evidence: `_evidence_v24_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P25 Memory Editor and expanded V4/P2–P6 audit: source commit `37c681665ce9c22414d52e02b83e9a64eca8c2d1` (`feat(desktop): add in-product Memory editor`). Independent Memory Edit now uses a bounded product dialog for title, content, and tags, with cancel-without-write, in-dialog validation/error states, and the existing scope-bound `memory:update` typed IPC path. Existing Coworker private, Team shared, Project Memory, suggestion approval, source trace, pin/forget/delete, bounded Project Search, deep links, and restart persistence remain canonical and were rechecked. The expanded audit read the authoritative goal attachment and requirement-by-requirement canonical matrix: P2 real Provider execution and P6 formal release remain external-blocked; P3 local Skills/Playbooks/Routines, P4 secure Worker boundaries, P5 local target boundaries, and P6 reliability mechanics are locally proven. Corrected the canonical audit's §41 prose count from 12/4 to the row-level 11 Proven/5 External-blocked. Hidden Electron gate `npm run verify:p25-memory-editor` passed 17/17; focused Memory/Search suite passed 7/7; desktop full suite passed 292 with 0 skips; root suite passed 232 with 2 skips; `npm run check` passed with 212 files; secret scan and diff check clean. Evidence: `_evidence_p25_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P26 Artifact Export Copy: source commit `93d48bcf8db936151b446d5b13a1bd37c8fb5006` (`feat(desktop): export managed artifact copies`). The follow-up audit confirmed Voice persistent settings and Attention unified inbox/snooze/escalation were already implemented, while Artifact lacked a local export-copy path. Files & Artifacts now exposes a native-dialog `Export copy / 导出副本` action backed by the existing unique ArtifactStore; renderer IPC accepts only opaque `artifactId`, main rejects symbolic-link sources and files over the existing 50 MiB bound, cancellation performs no write, and only basename/bytes/id/version return. No second store, raw path API, archive/delete lifecycle, or external share authority was added. Hidden Electron gate `npm run verify:p26-artifact-export` passed 16/16; desktop full suite passed 294 with 1 skip; root suite passed 232 with 2 skips; focused Artifact/IPC suite passed 4/4; syntax, secret scan, and diff check clean. Evidence: `_evidence_p26_2026-09-03/verify-p26-artifact-export.json` and `.log`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P27 Artifact Retention Lifecycle: source commit `bf2a581db80384b1a72dd1f7eab0474968117c1b` (`feat(desktop): add artifact retention lifecycle`). Files & Artifacts now has Active/Archived/All views and family-level Archive/Restore using lifecycle metadata distinct from publication state. The existing unique append-only ArtifactStore remains authoritative: no managed bytes, versions, or lineage are deleted or rewritten; restart, source navigation, History, direct deep links, and bounded scope/team/channel/coworker/type filters remain safe. Renderer IPC accepts only opaque `artifactId` and public projections exclude raw paths, cwd, authority, session, and provider data. No physical Delete, external Share, permission expansion, or second store was added. Hidden Electron gate `npm run verify:p27-artifact-retention` passed 19/19; focused Artifact/IPC/Product Shell suite passed 22/22; desktop syntax check passed 218 files; secret scan and diff check clean. Evidence: `_evidence_p27_2026-09-03/verify-p27-artifact-retention.json` and `.log`; local unsigned, `publishEligible=false`. Full desktop/root suites were not rerun in this continuation per the requested focused-verification scope. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P28 Renderer dialog-debt audit and Project create slice: source commit `ba389519b941393b41f4eab91950992ff1b86a2e` (`feat(desktop): replace project creation prompt`). Audited user-facing renderer prompt/alert/confirm use and selected the core Projects `New Project` path, replacing its blocking name prompt with a bounded product dialog. The form validates blank names in-dialog, cancellation performs no write, submit keeps the existing name-only typed `project:create` contract and main-process-owned workspace allocation, and errors remain in the dialog. Hidden Electron gate `npm run verify:p28-project-create` passed 18/18; focused Project/Product Shell suite passed 15/15; syntax check passed 220 files; secret scan and diff check clean. Evidence: `_evidence_p28_2026-09-03/verify-p28-project-create.json` and `.log`; local unsigned, publish not enabled. Other residual dialogs (routine error fallback, legacy Team Pack edit, command-palette routine selection, legacy memory/destructive lifecycle flows, and update apply) remain explicitly deferred as separate bounded slices. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P29 Command Palette Routine Selector: source commit `3b47e8fc7c7c3d1ee8f96526ce1a19539404fcb8` (`feat(desktop): add command palette routine selector`). Replaced the blocking `window.prompt` Run Routine picker with a bounded product dialog that renders safe Routine names, schedule/status metadata, and explicit Archived/Disabled/Unavailable/Ready states. Only active, enabled, reference-valid Routines are selectable; cancel and no selection perform no run. Selection reuses the typed `palette:execute` → governed `routine-controller.runNow` → `JobController.submitJob` path, with visible success status and in-dialog failure handling. Stale and forged ids fail closed through the existing governed service. Hidden Electron gate `npm run verify:p29-routine-selector` passed 20/20; focused palette/routines/Product Shell suite passed 21/21; syntax check passed 222 files; secret scan and diff check clean. Evidence: `_evidence_p29_2026-09-03/verify-p29-routine-selector.json` and `.log`; local unsigned, `publishEligible=false`. Full desktop/root suites were not rerun in this continuation per the requested focused-verification scope. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P30 Routine card action feedback: source commit `c7228d10184c7ed04ec5d316a7d031a6d12cfc73` (`feat(desktop): add routine card action feedback`). Routines `Run now` and archived `Restore` now use in-card success/failure feedback with explicit dismiss controls, bounded sanitized errors, pending button disabling, duplicate-click suppression, retry-after-failure behavior, and routine-id keyed state that survives refresh/re-render without cross-card contamination. Existing typed preload/IPC, `RoutineController`, `JobController`, archive/restore lifecycle, and fail-closed stale/unavailable validation remain authoritative; no scheduler, executor, provider, workspace authority, or new IPC authority was added. Hidden Electron gate `npm run verify:p30-routine-actions` passed 33/33; focused palette/routines/Product Shell suite passed 22/22; syntax check passed 224 files; secret scan and diff check clean. Evidence: `_evidence_p30_2026-09-03/verify-p30-routine-actions.json`, `.log`, and `.png`; local unsigned, `publishEligible=false`. Full desktop/root suites were not rerun in this continuation per the requested focused-verification scope. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P31 Team Pack Gallery consistency slice: product source commit `a0630f2370bbae8c20428c30ca852f5800808bc3` (`fix(desktop): unify Team Pack gallery editing`); verifier correction commits `e88abdc2c0d5c6ee854921c511274bab8ce82d59`, `8ab0c0cbd3139c9be0ac128b837e797af3b3d57b` (`test(desktop): validate P31 gallery click expression`), `71c5dc38025bd2929f1354605e0f980e2cb070e4` (bounded native-input verifier path), `71e76be0a5ee789c9dd465ea97c8e54b7bb3036f` (scroll-then-native-input correction), `2000ed8afb0c06f3f77630dcd3dfca473e45befa` (renderer diagnostics and bounded state probe), and `4303ea707c511bc3498a04559b1ef0b308fa5d2b` (probe invoke wrapping correction). P28 inventory confirmed the embedded Product hubs Team Pack gallery is ordinarily reachable through `nav-product-hubs` and still contained the old custom-name `window.prompt` edit and clipboard export, while the dedicated Team Packs page already had the P21 structured editor and native file flow. The old renderer now delegates custom `Edit recipe` and `Export` to the dedicated controller's shared structured editor and `exportPackToFile` native JSON path through document events; it adds only an opaque card id for verification. First-party entries remain read-only and retain Duplicate guidance. No second store/service/editor or new authority was added. P31 static test passed 4/4; desktop full suite previously passed 300 with 1 skip; desktop syntax check passed 228 files; desktop secret scan and diff check clean. The single follow-up unsandboxed run loaded the app and passed the hidden-window, older-gallery reachability, and first-party read-only/Duplicate checks (3/4), then scrollIntoView placed Duplicate inside the viewport and native-input dispatch completed. The bounded probe returned `ok:true` with new custom id `custom-pack-ab7de519ac8f`, confirming Duplicate; the next existing card-state invoke failed with renderer `Uncaught SyntaxError: Unexpected token ')'`, with no `render-process-gone` event. Evidence: `_evidence_p31_2026-09-03/verify-p31-team-pack-gallery.json` and `.log` record the latest checks, coordinates, native-input completion marker, successful Duplicate probe, console, and health result. Real Electron Duplicate is verified; edit/export/import/restart click-path acceptance remains pending, and no further gate retries are authorized in this slice. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- Acceptance documents:
  - docs/acceptance/V4.3-P31-TEAM-PACK-GALLERY-CONSISTENCY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P30-ROUTINE-CARD-ACTIONS-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P29-ROUTINE-SELECTOR-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P28-PROJECT-CREATE-DIALOG-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P27-ARTIFACT-RETENTION-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P26-ARTIFACT-EXPORT-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P25-MEMORY-EDITOR-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P24-FILES-ARTIFACTS-COMPUTER-HISTORY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P22-PLAYBOOK-LIBRARY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P23-SKILL-LIBRARY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P21-TEAM-PACK-RECIPE-EDITOR-ACCEPTANCE-2026-09-03.md
  - `docs/acceptance/V4.3-P18-CHANNEL-UNREAD-NOTIFICATIONS-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P19-PRODUCT-BURST-AUDIT-CHANNEL-EDITOR-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P20-TEAM-PACK-FILE-FLOW-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P17-DESKTOP-NOTIFICATION-CENTER-ACCEPTANCE-2026-09-03.md`
