# Auto_Empire Desktop Handoff

Updated: 2026-09-03

- Branch: `codex/v4-review-directed-handoff-protocol`
- P17 source commits: `bbfab400d742c562aee2369b981c01c7ece1d2ba`, `6e309d3`, `f49f7d3`
- P18 source commit: `19d5421`
- P18 regression test commit: `b5295b2`
- Scope completed: P18 Trusted Production Channel-Unread Notification Producer, closing the explicit P17 limitation. Integrates ConversationStore message events via observer API in the main process. Implements fail-closed intent architecture where coworker messages notify only when explicit production call-site metadata sets `notifyChannelUnread === true`. Suppresses user self-messages, inactive coworkers, archived channels, and intermediate handoff/review/fanout protocol stages. Tested via the real coworker dispatcher multi-stage chain (Chief -> Researcher -> Coder -> Chief) and a controlled local-runtime fanout join flow. The multi-stage test retains per-flush 0/0/0/1 notification assertions; the fanout test records every coworker message's production options and proves six child/review/protocol messages use `notifyChannelUnread: false`, with only the final join using `true`, while the real producer/service yields exactly 1 coalesced notification. This fanout fixture is not a live-provider or packaged-Electron proof. Keeps `conversation:get` strictly read-only and resolves unread state solely via explicit bounded `conversation:acknowledge` IPC. Promise-chains acknowledgement in UI and synchronizes badge refresh with backend resolution. Preserves digest dedupe key storage (`k_[a-f0-9]{32}`), collision-resistant opaque notification IDs (`notif_[a-f0-9]{16}`), and trusted-boundary redaction of secrets and absolute paths.
- Acceptance: `npm run verify:p18-channel-unread` → 10/10 PASS; `npm run verify:p17-notification-center` → 20/20 PASS; `node --test desktop/test/p18-channel-unread.test.mjs` → 9/9 PASS; `node --test desktop/test/collaboration-ledger.test.mjs` → 15/15 PASS; `npm test` (desktop) → 276 tests: 275 pass, 1 skipped, 0 fail; `npm test` (root) → 234/234 PASS (232 pass, 2 skipped, 0 fail); `npm run check` → syntax ok, 192 files; `npm run secret-scan` → clean; `git diff --check` → clean.
- Evidence: `_evidence_v57_2026-09-03/`; hidden Electron gate remains 10/10 PASS, and `p18-local-verification.json` records the current 9/9 focused P18 suite, 15/15 collaboration ledger, full-suite counts, and observed fanout intent boundary. Evidence is local, unsigned, and `publishEligible=false`; the fanout summary is a controlled local-runtime proof, not a live-provider or packaged-Electron proof. The evidence commit is the final commit containing this handoff, the P18 acceptance document, and the evidence directory; its SHA is reported in the delivery summary.
- Channel-unread status: P17 limitation closed. Automated trusted main-process producer is active and verified across real multi-stage dispatcher chains and hidden Electron runtime.
- No provider, network, OAuth, cloud, remote, packaging, deployment, or account action was performed.
- P19 Product Burst audit and slice: audited Playbook Library, Files & Artifacts Hub, Computer Activity History, Skill Library, Team Pack Gallery, Channels, and External Control. Implemented the highest-priority complete local slice: independent Channels page New/Edit now uses the governed full channel dialog with Team context, kind, instructions, shared workspace, and Playbook, then refreshes the page after persistence. Removed the limited prompt fallback and added async error handling. Source commit: `8725ade` (`Complete product channel editor flow`).
- P19 acceptance: real Electron `npm run verify:software-team` passed, including `P0_CHANNEL_PRODUCT_EDITOR_ROUNDTRIP`; desktop `npm run check` passed (192 files); focused Product Shell test passed 13/13; desktop `npm test` passed 275 with 1 Docker-dependent skip and 0 failures. Evidence: `_evidence_v58_2026-09-03/channel-editor-local-verification.json`; docs/evidence commit is the final commit containing the audit document, evidence record, and this handoff.
- P19 remaining priority order: production secure paired/relay External Control lifecycle; Team Pack file-picker/download roundtrip; independent Files/Artifacts, Computer History, and Skills UI/capability-boundary gates; then Playbook/Channels archive/template click-path gates. No package/deploy/publish/main merge or user account/browser operation was performed.
- P20 Team Pack local file flow: native main-process open/save dialogs now replace independent Gallery clipboard/prompt JSON import/export. File content is bounded to 64 KiB, validated through the existing declarative `team:importPack` schema, rejects authority-bearing fields, sanitizes file I/O errors, and exposes only basenames to the renderer. Gallery refreshes after import and preserves existing Install, Duplicate, and custom Edit service paths. Source commit: `839a314` (`Complete Team Pack local file flow`).
- P20 acceptance: `npm run verify:p20-team-packs` → hidden Electron gate 23/23 PASS; `npm test` (desktop) → 278 pass, 1 skipped, 0 fail; `npm run check` → syntax ok, 194 files; `npm run secret-scan` → clean; `git diff --check` → clean. Evidence: `_evidence_v20_2026-09-03/`; local unsigned evidence with `publishEligible=false`. The existing custom Edit native prompt remains unchanged; the hidden gate proves Edit control reachability and the same preload IPC persistence contract separately because the hidden Electron runner cannot inject native prompt text.
- P21 Team Pack structured recipe editor: source commit 72897c2 (Implement structured Team Pack recipe editor). Independent Gallery custom/imported Edit now opens a fixed product-internal form for pack metadata, coworker names/roles/instructions and safe model profiles, channel metadata/instructions/playbook links, and playbook metadata/ordered steps/semantic fields; bounded add/remove actions are supported. First-party recipes remain read-only and are duplicated before editing. Save uses the existing team:editPack preload/IPC path, refreshes after success, surfaces errors in-dialog, and cancel performs no write. Hidden Electron real click-path gate passed 27/27 for both verify:p20-team-packs and verify:p21-team-packs; it verified full composition persistence, refresh, cancel no-write, authority-bearing import rejection, and isolated restart persistence. Desktop full suite: 278 pass, 1 skipped, 0 fail; focused Product Shell: 13/13 PASS; check: 194 files; secret scan and diff check clean. Evidence: _evidence_v21_2026-09-03/; local unsigned, publishEligible=false. No provider, network, account, browser, external-control, packaging, deployment, or publish action was performed.
- P22 Playbook Library structured editor and native file flow: source commit `a35e8d0` (`Implement Playbook Library structured editor and file flow`). Independent Playbooks page New/Edit now uses a fixed form for name, description, ordered steps, stages, review points, expected output, recommended coworker roles, and recommended Skills; bounded add/remove/reorder controls preserve the existing backend limits, Cancel performs no write, and errors remain in-dialog. Team/Channel assignment remains typed and archived Playbooks hide assignment controls. Duplicate, Archive, and Restore use the existing service path. Native main-process Electron dialog helpers replace renderer JSON prompt/clipboard import/export, cap files at 64 KiB, validate `sovereignbot.desktop.playbook.v1`, return only basenames/status, and reject authority/path/session/token material. Fixed reload validation preserves unassigned duplicate/import entries across restart. Hidden Electron gate `npm run verify:p22-playbooks` passed 11/11; focused suite passed 31/31; `npm run check` passed with 199 files; desktop full suite passed 282 with 1 skip; root suite passed 232 with 2 skips; secret scan clean. Evidence: `_evidence_v22_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- P23 Skill Library structured editor and native file flow: source commit `0be3077fa131b9f377da48c0bb5b9094bdf9a335` (`feat(desktop): complete structured Skill Library path`). Independent Skills page New/Edit now uses a structured form for name, description, instructions, source/type, inputs, ordered steps, expected output, validators, and requested capability declarations; bounded add/remove/reorder controls preserve backend limits, Cancel performs no write, and errors remain in-dialog. Archived Skills are read-only until restored. Duplicate, Archive/Restore, Retest definition, Team/Coworker assign/unassign, and Create Routine remain wired to typed service/event paths. Native main-process JSON import/export is capped at 64 KiB, returns only basename/status data to the renderer, validates the declarative `sovereignbot.desktop.skill.v1` shape, and rejects authority/path/session/token/credential/URL material; requested capabilities never grant access. Fixed governed-retest persistence so `lastTestedAt` and `updatedAt` survive restart while lifecycle metadata stays separate from the portable definition. Hidden Electron gate `npm run verify:p23-skills` passed 14/14; focused suite passed 21/21; `npm run check` passed with 205 files; desktop full suite passed 288 with 1 skip; root suite passed 232 with 2 skips; secret scan and diff check clean. Evidence: `_evidence_v23_2026-09-03/`; local unsigned, `publishEligible=false`. No provider, network, OAuth, account, browser, external-control, packaging, deployment, publish, or main-merge action was performed.
- Acceptance documents:
  - docs/acceptance/V4.3-P22-PLAYBOOK-LIBRARY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P23-SKILL-LIBRARY-ACCEPTANCE-2026-09-03.md
  - docs/acceptance/V4.3-P21-TEAM-PACK-RECIPE-EDITOR-ACCEPTANCE-2026-09-03.md
  - `docs/acceptance/V4.3-P18-CHANNEL-UNREAD-NOTIFICATIONS-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P19-PRODUCT-BURST-AUDIT-CHANNEL-EDITOR-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P20-TEAM-PACK-FILE-FLOW-ACCEPTANCE-2026-09-03.md`
  - `docs/acceptance/V4.3-P17-DESKTOP-NOTIFICATION-CENTER-ACCEPTANCE-2026-09-03.md`
